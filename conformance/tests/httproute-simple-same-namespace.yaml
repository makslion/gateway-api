apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gw-conf-infra-test
  namespace: gw-conf-infra
spec:
  parentRefs:
  - name: same-namespace
  rules:
  - backendRefs:
    - name: infra-backend-v1
      port: 8080
